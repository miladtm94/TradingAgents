# Web console runtime. Install this repository's TradingAgents package first;
# the adapter and web console must use the same local code version.
fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
cryptography==44.0.0
pydantic==2.10.4
